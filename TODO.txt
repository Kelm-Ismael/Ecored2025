UPDATE 8/OCT (que falta)
    - crear usuarios desde web (para crear usuario escuela admin) ✓
    - buscar escuela + solicitar linkto escuela (usuario ciudadano a usuario alumno) ✓
    - aceptar/rechazar alumnos -> cambio tipo user  ✓
    - sumar pts a escuela en entrega ✓

NOTAS
    - la linea 23:
        import InformacionStack from './navigation/informacionStack';  //error en web
    en /frontend/App.js genera un error en web cuando se arma la pag o se actualiza. 
    para hacer funcionar el sistema web comentar la linea y si se quiere ver la app descomentarla. 
    el manejar error que le puse no funciona.